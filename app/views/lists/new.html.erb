 <div class="container">

  <h1>Add a new list</h1>

 <%= form_for(@list) do |f| %>

    <p>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </p>
      <%= f.submit %>
    <% end %>

    <%= link_to 'Back to lists', lists_index_path  %>

</div>
